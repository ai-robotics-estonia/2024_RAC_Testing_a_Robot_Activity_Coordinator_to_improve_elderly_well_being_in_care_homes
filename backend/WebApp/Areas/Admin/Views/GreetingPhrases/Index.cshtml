@model IEnumerable<App.Domain.GreetingPhrase>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Phrase)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RobotMapApp)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GreetingPhraseCategory)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Phrase)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RobotMapApp!.DisplayName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GreetingPhraseCategory!.CategoryName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
